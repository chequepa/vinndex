# macOS
.DS_Store
.AppleDouble
.LSOverride

# Claude Code local config
.claude/

# Editors
.vscode/
.idea/
*.swp
*.swo

# Node
node_modules/
.next/
out/
dist/
build/
.turbo/
coverage/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# Intermediate scrape snapshots (merged into data/snapshot.json)
/data/snapshot-tiendanube.json
/data/snapshot-woocommerce.json
/data/snapshot-vtex.json
/data/snapshot-shopify.json
/data/snapshot-mercadolibre.json
/data/snapshot-magento.json
/data/snapshot-prestashop.json
/data/snapshot-ambar.json

# Stage 2 embeddings cache (restored via actions/cache in CI)
/data/embeddings-cache.json

# Stage 3 LLM adjudicator cache (restored via actions/cache in CI)
/data/stage3-cache.json

# Stage 3 gray-zone candidates (emitted by Stage 2, consumed by Stage 3)
/data/stage3-candidates.json

# Server-side pageview store (regenerated at runtime, persists during
# container life only).
/data/pageviews.ndjson

# Env files (secrets!)
.env
.env.local
.env.*.local
.env.production

# Logs
*.log
npm-debug.log*

# OS
Thumbs.db
/data/image-url-candidates.json
/data/phash-cache.ndjson
/data/phash-candidates.json

/eng.traineddata
/spa.traineddata
